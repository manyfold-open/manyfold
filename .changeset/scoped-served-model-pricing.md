---
'@manyfold/api': patch
'@manyfold/web': patch
---

Resolve managed model prices and pins within the served channel, preserve the verified provider scope across interrupted coding turns, and keep provider-specific prices isolated from other channels. Platform Gemini machine turns preserve native configuration and system policy through isolated temporary settings.
