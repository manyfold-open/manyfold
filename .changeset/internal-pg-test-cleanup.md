---
---

The API changes only close PostgreSQL test harness connections, remove forced test exits from verification tooling, and correct an A2A command name in a source comment. They do not change the API runtime contract. The CLI command removal and Web guidance change have their own release note.
