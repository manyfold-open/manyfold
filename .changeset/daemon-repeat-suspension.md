---
"@manyfold/api": patch
---

Keep daemon chat turns suspended across repeated disconnects instead of marking an unfinished response complete when a replayed output identity deduplicates its suspension event.
