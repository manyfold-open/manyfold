---
'@manyfold/web': minor
---

Update Center batches keep running after you leave the page. The queue now lives outside the route, so the per-row states and the "N updated · M failed" summary are still there when you come back, and the account menu's Updates entry shows "Updating" while a batch runs.
