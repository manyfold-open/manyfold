---
'@manyfold/admin': minor
---

The hermes dashboard Enable/Disable and Open controls are shown only for sprite runtimes; k8s runtime rows keep showing the (always disabled) dashboard fact, since k8s dashboard hosting was removed.
