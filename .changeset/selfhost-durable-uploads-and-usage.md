---
'@manyfold/api': patch
'@manyfold/web': patch
---

Persist self-host chat uploads across API container replacement and restrict the default CORS allowlist to the configured Web and Admin origins. Self-host accounts can inspect their effective resource quotas and usage without cloud billing actions.
