---
'@manyfold/api': patch
---

Admit git framework versions and their source repositories together before agent creation, sandbox preparation and upgrades. Reject unavailable repository pins and prevent installation failures from retrying an unadmitted git default, while preserving npm and image fallback behavior.
