---
'@manyfold/web': patch
---

Pi agents show Pi's own logo, the `Pi` mark from LobeHub's icon set, instead of a hand-drawn π that was not Pi's mark. Like the Hermes mark, it takes the text color, so it stays legible in light and dark mode.
