---
'@manyfold/api': patch
---

Install configured default skills on runtime-attached agents, including daemon, existing sprites, and Kubernetes agents. Preserve existing skill intents and report installation failures without failing agent creation.
