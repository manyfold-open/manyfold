---
'@manyfold/web': patch
---

Re-export the shared social card as v6: the landing register on a dark ground, with a Fieldwork ripple beside the hero copy and the milled-aluminium chassis retired. Both apps point at the new file; v5 keeps its own URL and bytes for clients that cached it.
