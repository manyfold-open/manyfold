---
"@manyfold/api": minor
"@manyfold/web": minor
"@manyfold/cli": patch
---

Add a shared Claude Code/Codex plugin for mf-powered platform operations,
skill-maintained workbench route rules, and live automation updates in
the workbench across API instances. Unify the standalone and plugin
manyfold-cli-usage skill, with identity-aware guidance and complete
reference bundles while retaining its default-install identity.
