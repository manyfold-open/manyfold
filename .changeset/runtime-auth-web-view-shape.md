---
---

No user-visible change in `@manyfold/web`: the create wizard's draft model-config view and two test fixtures gained the `runtimeAuth` block that `AgentModelConfigView` now requires (runtime auth profiles P2). The binding UI itself ships in P3 with its own changeset.
