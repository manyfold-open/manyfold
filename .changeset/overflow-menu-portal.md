---
'@manyfold/web': patch
---

Row action menus (the "…" button) now open above any card that clips its contents, so the menu on an added account of a runtime is visible again instead of being cut off at the card edge.
