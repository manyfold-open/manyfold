import type { FC, ReactNode } from 'react'

// Editions slot (§3.2.2 搬迁表: the UserDetail commercial cards): managed
// credit account, recharge records, payments and subscriptions. The
// open-source build has no such data; the cloud overlay shadows this with
// the full section.
const UserCommerceSection: FC<{ userId: string }> = (): ReactNode => null

export default UserCommerceSection
