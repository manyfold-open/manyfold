import type { FC, ReactNode } from 'react'

// Slot (editions §3.4): the managed-provider credit settings editor is a cloud
// commerce surface. The cloud build shadows this file via the admin overlay.
const UsersCreditSettingsCard: FC = (): ReactNode => null

export default UsersCreditSettingsCard
