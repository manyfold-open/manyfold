import type { FC, ReactNode } from 'react'

// Slot (editions §3.4): granting managed containers is a cloud commerce
// surface. The cloud build shadows this file via the admin overlay.
const UserContainersSection: FC<{ userId: string }> = (): ReactNode => null

export default UserContainersSection
