---
version: '4.5.0'
date: '2026-09-24'
---

`mf agent help` now describes `--framework` by example. It names the core
frameworks (`openclaw`, `hermes`, `claude-code`, `codex`, `gemini-cli` and
`pi`) instead of presenting a closed list, because an edition of the platform
can register frameworks of its own.
