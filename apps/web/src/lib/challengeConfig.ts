// Editions slot (§3.3): route constants only — the shell's marketing footer
// links the path, and the page slots behind it redirect home on a
// self-hosted deployment. The cloud overlay shadows this module with the
// full campaign configuration.
export const CHALLENGE_PATH = '/challenge'
