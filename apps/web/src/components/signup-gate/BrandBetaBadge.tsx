import type { FC } from 'react'

// Editions slot (§3.3): the marketing-surface beta badge of the cloud
// gated rollout; the cloud overlay shadows this with the rendered span.
const BrandBetaBadge: FC = () => null

export default BrandBetaBadge
