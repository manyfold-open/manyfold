import type { FC } from 'react'

// Editions slot (§3.3): the beta badge belongs to the cloud gated rollout;
// the cloud overlay shadows this with the rendered tag.
const SignupBetaBadge: FC = () => null

export default SignupBetaBadge
